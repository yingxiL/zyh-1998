<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.demo.img.mapper.ImgMapper">
    <select id="select" resultType="com.demo.img.entity.Img">
        select i_name from img
    </select>
    <select id="select1" resultType="com.demo.img.entity.Img">
        select i_title from img
    </select>
    <select id="select2" resultType="com.demo.img.entity.Img">
        select i_img from img
    </select>
    <select id="select3" resultType="com.demo.img.entity.Img">
        select i_img1 from img
    </select>
</mapper>